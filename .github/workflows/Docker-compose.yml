version: "3.9"
services:
  mkdocs:
    build: .
    container_name: mkdocs
    ports:
      - 80:80
      - 433:433
    volumes:
      - ../app:/app
      - ../docs:/docs
      - ../launcher:/launcher
    working_dir: /docs
    command:
      sh -c "mkdocs gh-deploy --force --config-file 'docs/mkdocs.yml'"
      #sh -c "while true; do moo; sleep 2; done"